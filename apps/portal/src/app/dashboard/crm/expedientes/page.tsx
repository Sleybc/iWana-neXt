import { ExpedientesLandingClient } from '@/components/crm/expedientes/ExpedientesLandingClient';

export const metadata = {
  title: 'Oportunidades | Portal Empresarial',
  description: 'Captación, calificación y cierre comercial de la empresa',
};

export default function ExpedientesPage() {
  return <ExpedientesLandingClient />;
}
